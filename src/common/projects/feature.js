import React from 'react';
import './feature.scss';

//if string or array render to paragrpahs
// if object assume two keys = name and paragraphs. render special.

export default class Feature extends React.Component {
    render(){
        return null;
    }
}